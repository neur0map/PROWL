Launch a new agent that has access to the following tools: prowl_agent, glob, grep, ls, view. When you are searching for a keyword or file and are not confident that you will find the right match on the first try, use the agent tool to perform the search for you. The sub-agent answers structural questions with prowl_agent's cited index and reserves grep/glob for literal text and filename patterns.
