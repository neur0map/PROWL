Find files by name/pattern (glob syntax), sorted by modification time; max {{ .MaxResults }} results; skips hidden files. Use grep to search file contents.

Scope: filename patterns only. To find where a symbol, feature, or setting lives, use prowl_agent (`find`, `search`) — it answers with `file:line` citations instead of a list of candidate paths to open.
